[package]
name = "transport-only-consumer"
version = "0.0.0"
edition = "2021"
publish = false

# Standalone fixture: resolves outside the workspace's feature unification.
[workspace]

[dependencies]
pulsehive = { path = "../../../pulsehive", features = ["openai"] }
